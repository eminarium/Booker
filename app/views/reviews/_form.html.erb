<%= form_with(model: [@book, review], local: true) do |form| %>
<%#= form_for ([@book, @review]) do |form| %>
  <% if review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= form.label "Review Note", class: "col-sm-2 col-form-label" %>
    <div class="col-sm-4">
      <%= form.text_area :review_note, class: "form-control", id: "review_note", placeholder: "Review Note" %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label "Rating", class: "col-sm-2 col-form-label" %>
    <div class="col-sm-4" id='star-rating'></div>
  </div>

  <button type="submit" class="btn btn-primary">Submit</button>
  
<% end %>

<script>
 $('#star-rating').raty({
  path: '/assets/',
  scoreName: 'review[rating]'
 });
</script>